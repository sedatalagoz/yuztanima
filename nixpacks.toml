[phases.setup]
nixPkgs = ['python310', 'cmake', 'gcc', 'g++']

[phases.install]
cmds = ['pip install --upgrade pip', 'pip install -r requirements_web.txt']

[phases.build]
cmds = []

[start]
cmd = 'gunicorn -w 4 -b 0.0.0.0:$PORT web_app:app'
